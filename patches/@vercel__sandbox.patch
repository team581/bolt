diff --git a/dist/api-client/api-client.cjs b/dist/api-client/api-client.cjs
index a1ba371ec94f562b3ac8ddcdada8d62fd6cee2fc..9e50595ec5f2430aeec6ad398d949415acf8e93b 100644
--- a/dist/api-client/api-client.cjs
+++ b/dist/api-client/api-client.cjs
@@ -120,10 +120,16 @@ var APIClient = class extends require_base_client.BaseClient {
 				signal: params.signal
 			});
 			if (!response.ok) await require_base_client.parseOrThrow(zod.z.any(), response);
-			if (response.headers.get("content-type") !== "application/x-ndjson") throw new require_api_error.APIError(response, {
-				message: "Expected a stream of command data",
-				sessionId: params.sessionId
-			});
+			if (response.headers.get("content-type") !== "application/x-ndjson") {
+				const actualContentType = response.headers.get("content-type") ?? "<missing>";
+				let bodyText = "";
+				try { bodyText = await response.text(); } catch {}
+				throw new require_api_error.APIError(response, {
+					message: `Expected a stream of command data; got content-type=${actualContentType} status=${response.status} body=${bodyText.slice(0, 1024)}`,
+					text: bodyText,
+					sessionId: params.sessionId
+				});
+			}
 			if (response.body === null) throw new require_api_error.APIError(response, {
 				message: "No response body",
 				sessionId: params.sessionId
diff --git a/dist/api-client/api-client.js b/dist/api-client/api-client.js
index 5b3933842c4bacb7f6539552d18ff28f7de10073..ce4229711973cb2549e63fbd934b05fe49afb703 100644
--- a/dist/api-client/api-client.js
+++ b/dist/api-client/api-client.js
@@ -117,10 +117,16 @@ var APIClient = class extends BaseClient {
 				signal: params.signal
 			});
 			if (!response.ok) await parseOrThrow(z.any(), response);
-			if (response.headers.get("content-type") !== "application/x-ndjson") throw new APIError(response, {
-				message: "Expected a stream of command data",
-				sessionId: params.sessionId
-			});
+			if (response.headers.get("content-type") !== "application/x-ndjson") {
+				const actualContentType = response.headers.get("content-type") ?? "<missing>";
+				let bodyText = "";
+				try { bodyText = await response.text(); } catch {}
+				throw new APIError(response, {
+					message: `Expected a stream of command data; got content-type=${actualContentType} status=${response.status} body=${bodyText.slice(0, 1024)}`,
+					text: bodyText,
+					sessionId: params.sessionId
+				});
+			}
 			if (response.body === null) throw new APIError(response, {
 				message: "No response body",
 				sessionId: params.sessionId
