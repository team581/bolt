diff --git a/dist/index.cjs b/dist/index.cjs
index e47bdceb013ec78454d16a95fbec7752a1f77329..4a073bb58eed1322ae7cac10da1e400c6d1f7cab 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -20,6 +20,7 @@ function shellQuote(arg) {
 // src/sandbox/mounts/types.ts
 var LOG_PREFIX = "[@mastra/daytona]";
 var SAFE_BUCKET_NAME = /^[a-z0-9][a-z0-9.\-]{1,61}[a-z0-9]$/;
+var SAFE_GCS_BUCKET_NAME = /^[a-z0-9][a-z0-9._\-]{1,61}[a-z0-9]$/;
 function validateBucketName(bucket) {
   if (!SAFE_BUCKET_NAME.test(bucket)) {
     throw new Error(
@@ -27,6 +28,13 @@ function validateBucketName(bucket) {
     );
   }
 }
+function validateGCSBucketName(bucket) {
+  if (!SAFE_GCS_BUCKET_NAME.test(bucket)) {
+    throw new Error(
+      `Invalid GCS bucket name: "${bucket}". Bucket names must be 3-63 characters, lowercase alphanumeric, hyphens, underscores, or dots.`
+    );
+  }
+}
 function validateEndpoint(endpoint) {
   let parsed;
   try {
@@ -165,7 +173,7 @@ Sandbox network response: ${checkOutput}` : "")
 }
 async function mountGCS(mountPath, config, ctx) {
   const { run, writeFile, logger } = ctx;
-  validateBucketName(config.bucket);
+  validateGCSBucketName(config.bucket);
   const quotedMountPath = shellQuote(mountPath);
   const connectivityCheck = await run("curl -sS --max-time 5 http://storage.googleapis.com 2>&1", 1e4);
   const checkOutput = connectivityCheck.stdout.trim();
diff --git a/dist/index.js b/dist/index.js
index f539b18309508fc43a77b9b8ba4bff0fcf5f422c..1777d62e74882271042b786170ecd9ce3470afcb 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -18,6 +18,7 @@ function shellQuote(arg) {
 // src/sandbox/mounts/types.ts
 var LOG_PREFIX = "[@mastra/daytona]";
 var SAFE_BUCKET_NAME = /^[a-z0-9][a-z0-9.\-]{1,61}[a-z0-9]$/;
+var SAFE_GCS_BUCKET_NAME = /^[a-z0-9][a-z0-9._\-]{1,61}[a-z0-9]$/;
 function validateBucketName(bucket) {
   if (!SAFE_BUCKET_NAME.test(bucket)) {
     throw new Error(
@@ -25,6 +26,13 @@ function validateBucketName(bucket) {
     );
   }
 }
+function validateGCSBucketName(bucket) {
+  if (!SAFE_GCS_BUCKET_NAME.test(bucket)) {
+    throw new Error(
+      `Invalid GCS bucket name: "${bucket}". Bucket names must be 3-63 characters, lowercase alphanumeric, hyphens, underscores, or dots.`
+    );
+  }
+}
 function validateEndpoint(endpoint) {
   let parsed;
   try {
@@ -163,7 +171,7 @@ Sandbox network response: ${checkOutput}` : "")
 }
 async function mountGCS(mountPath, config, ctx) {
   const { run, writeFile, logger } = ctx;
-  validateBucketName(config.bucket);
+  validateGCSBucketName(config.bucket);
   const quotedMountPath = shellQuote(mountPath);
   const connectivityCheck = await run("curl -sS --max-time 5 http://storage.googleapis.com 2>&1", 1e4);
   const checkOutput = connectivityCheck.stdout.trim();
