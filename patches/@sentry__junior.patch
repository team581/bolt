diff --git a/dist/app.js b/dist/app.js
index f3f781b55c31c640b24411301b9b62e22b50b689..b5787a09766b396a5e6c5397759ac776cb0bc338 100644
--- a/dist/app.js
+++ b/dist/app.js
@@ -12746,9 +12746,38 @@ async function generateAssistantReply(messageText2, context = {}) {
       userInput,
       promptConversationContext
     );
+    let resolvedUserAttachments = context.userAttachments;
+    if (context.resolveAttachmentsLazily) {
+      const sandboxAttachmentWriter = async ({ filename, mediaType, data }) => {
+        const workspaceSandbox = await getSandbox({
+          trigger: "attachment.upload",
+          path: filename
+        });
+        const sandboxCredentials = getVercelSandboxCredentials();
+        const rawSandbox = await Sandbox.get({
+          name: workspaceSandbox.sandboxId,
+          ...sandboxCredentials ?? {}
+        });
+        const safeName = (filename ?? "attachment").replace(
+          /[^A-Za-z0-9._-]/g,
+          "_"
+        );
+        const uploadDir = `${SANDBOX_WORKSPACE_ROOT}/uploads`;
+        const sandboxPath = `${uploadDir}/${safeName}`;
+        try {
+          await rawSandbox.mkDir(uploadDir);
+        } catch {
+        }
+        await rawSandbox.writeFiles([{ path: sandboxPath, content: data }]);
+        return sandboxPath;
+      };
+      resolvedUserAttachments = await context.resolveAttachmentsLazily(
+        sandboxAttachmentWriter
+      );
+    }
     const { routerBlocks, userContentParts } = buildUserTurnInput({
       omittedImageAttachmentCount: context.omittedImageAttachmentCount ?? 0,
-      userAttachments: context.userAttachments,
+      userAttachments: resolvedUserAttachments,
       userTurnText
     });
     const preAgentPromptMessages = () => existingSessionRecord?.piMessages ?? [
@@ -19338,6 +19367,41 @@ async function resolveUserAttachmentsWithDeps(attachments, context, deps) {
         data = attachment.data;
       }
       if (!data) continue;
+      if (context.sandboxWriter) {
+        const sandboxPath = await context.sandboxWriter({
+          filename: attachment.name,
+          mediaType,
+          data
+        });
+        logInfo(
+          "attachment_uploaded_to_sandbox",
+          {
+            slackThreadId: context.threadId,
+            slackUserId: context.requesterId,
+            slackChannelId: context.channelId,
+            runId: context.runId,
+            assistantUserName: botConfig.userName,
+            modelId: botConfig.modelId
+          },
+          {
+            "file.size": data.byteLength,
+            "file.mime_type": mediaType,
+            "file.path": sandboxPath
+          },
+          "Uploaded user attachment into the sandbox"
+        );
+        resolvedAttachment.promptText = [
+          "<attachment>",
+          `filename: ${attachment.name ?? "unnamed"}`,
+          `media_type: ${mediaType}`,
+          `sandbox_path: ${sandboxPath}`,
+          "encoding: file",
+          `size: ${data.byteLength}`,
+          "</attachment>"
+        ].join("\n");
+        results.push(resolvedAttachment);
+        continue;
+      }
       if (data.byteLength > MAX_USER_ATTACHMENT_BYTES) {
         logWarn(
           "attachment_skipped_size_limit",
@@ -20168,17 +20232,18 @@ function createReplyToThread(deps) {
           message,
           options.queuedMessages
         );
-        const userAttachments = await deps.resolveUserAttachments(
-          turnAttachments,
-          {
+        const resolveAttachmentsLazily = async (sandboxWriter) => {
+          return await deps.resolveUserAttachments(turnAttachments, {
             threadId,
             requesterId: message.author.userId,
             channelId,
             runId,
             conversation: preparedState.conversation,
-            messageTs: slackMessageTs
-          }
-        );
+            messageTs: slackMessageTs,
+            sandboxWriter
+          });
+        };
+        const userAttachments = void 0;
         const omittedImageAttachmentCount = !isVisionEnabled() && hasPotentialImageAttachment(turnAttachments) ? countPotentialImageAttachments(turnAttachments) : 0;
         const status = createSlackAdapterAssistantStatusSession({
           channelId: assistantThreadContext?.channelId,
@@ -20271,6 +20336,7 @@ function createReplyToThread(deps) {
               inboundAttachmentCount: turnAttachments.length,
               omittedImageAttachmentCount,
               userAttachments,
+              resolveAttachmentsLazily,
               correlation: {
                 conversationId,
                 threadId,
@@ -21280,7 +21346,7 @@ async function handleUnlink(event, provider) {
     "slash_command_unlink",
     { slackUserId: event.user.userId },
     { "app.credential.provider": provider },
-    `Unlinked ${formatProviderLabel(provider)} account via /jr slash command`
+    `Unlinked ${formatProviderLabel(provider)} account via /bolt slash command`
   );
   await postEphemeral(
     event,
@@ -21292,12 +21358,12 @@ async function handleSlashCommand(event) {
   if (!subcommand || !["link", "unlink"].includes(subcommand)) {
     await postEphemeral(
       event,
-      "Usage: `/jr link <provider>` or `/jr unlink <provider>`"
+      "Usage: `/bolt link <provider>` or `/bolt unlink <provider>`"
     );
     return;
   }
   if (!provider || rest.length > 0) {
-    await postEphemeral(event, `Usage: \`/jr ${subcommand} <provider>\``);
+    await postEphemeral(event, `Usage: \`/bolt ${subcommand} <provider>\``);
     return;
   }
   const normalized = provider.toLowerCase();
@@ -21375,7 +21441,7 @@ function registerProductionHandlers(bot, slackRuntime) {
     (event) => slackRuntime.handleAssistantContextChanged(event)
   );
   bot.onSlashCommand(
-    "/jr",
+    "/bolt",
     (event) => withSpan(
       "chat.slash_command",
       "chat.slash_command",
diff --git a/dist/chat/ingress/slash-command.d.ts b/dist/chat/ingress/slash-command.d.ts
index fce314aa9a4c7e28947eb9320b564c2ba3f329ae..01f5d376654d9239543b95ed391845c437585380 100644
--- a/dist/chat/ingress/slash-command.d.ts
+++ b/dist/chat/ingress/slash-command.d.ts
@@ -1,3 +1,3 @@
 import type { SlashCommandEvent } from "chat";
-/** Route `/jr link` and `/jr unlink` slash commands to the appropriate OAuth flow. */
+/** Route `/bolt link` and `/bolt unlink` slash commands to the appropriate OAuth flow. */
 export declare function handleSlashCommand(event: SlashCommandEvent): Promise<void>;
