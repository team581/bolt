---
name: manage-github-projects
description: Use when working with Team 581 GitHub Projects and issues. Complements github-issues with workflows for creating or updating Team 581 issues, issue types, priorities, deadlines, milestones, project status, and board items.
license: MIT
---

# Manage GitHub Projects

Drive Team 581's GitHub Projects (v2) boards through the `gh project` CLI.

For issue creation, updates, comments, or triage, also load the `github-issues` skill and apply its issue-writing and GitHub CLI conventions. This skill remains the source of truth for Team 581-specific org/project metadata: issue types, issue fields, sub-issues, and GitHub Projects fields.

## Team 581 conventions

- The org is always `team581`. Pass `--owner team581` on every `gh project` command.
- Projects belong to the org, not a repo. The repo on an issue/PR is independent of which project it's on.
- The active project is **{{project.title}}** (`--number {{project.number}}`, project ID `{{project.id}}`, <{{project.url}}>).

## Active project: {{project.title}}

Issue field IDs, project field IDs, and single-select option IDs below are baked in for the {{project.title}} project and `team581` org.

## Issue types

Enabled issue types for the `team581` org:

| Name | ID | Description |
| --- | --- | --- |
{{#each issueTypes}}
| {{name}} | `{{id}}` | {{description}} |
{{/each}}

When creating or editing issues, always assign a type if it's obvious which one to use. Leave blank otherwise. Use type names from the table with `gh issue create --type <name>`, `gh issue edit <issue> --type <name>`, `gh issue edit <issue> --remove-type`, and `gh issue list --type <name>`.

## Issue fields

Prefer organization-level issue fields instead over project fields when possible.

| Field | Field ID |
| --- | --- |
{{#each issueFields}}
| {{name}} | `{{id}}` |
{{/each}}

{{#each issueSingleSelectFields}}
{{name}} options:

| Name | Option ID |
| --- | --- |
{{#each options}}
| {{name}} | `{{id}}` |
{{/each}}

{{/each}}
Set an issue field value on an issue:

{{#if issueFieldExample}}
```sh
gh api --method POST \
  --header "X-GitHub-Api-Version: 2026-03-10" \
  /repos/<owner>/<repo>/issues/<issue-number>/issue-field-values \
  --input - <<'JSON'
{{json issueFieldExample}}
JSON
```

{{/if}}
For single-select issue fields, use the option name as the value. Clear a field with:

```sh
gh api --method DELETE \
  --header "X-GitHub-Api-Version: 2026-03-10" \
  /repos/<owner>/<repo>/issues/<issue-number>/issue-field-values/<field-id>
```

## Project fields

Use project fields for project-only metadata.

| Field | Field ID |
| --- | --- |
{{#each fields}}
| {{name}} | `{{id}}` |
{{/each}}

{{#each singleSelectFields}}
{{name}} options:

| Name | Option ID |
| --- | --- |
{{#each options}}
| {{name}} | `{{id}}` |
{{/each}}

{{/each}}
Item IDs are per-item and always have to be looked up. List the board's items to find the one you want:

```sh
gh project item-list {{project.number}} --owner team581 --format json --limit 200
```

## Operations

Add an issue or PR (works for both URL types):

```sh
gh project item-add {{project.number}} --owner team581 --url <issue-or-pr-url>
```

Manage issue hierarchy with `gh issue edit` (issue arguments can be numbers or URLs):

- `--parent <issue>` sets or changes a parent.
- `--remove-parent` removes a parent.
- `--add-sub-issue <issue>` adds a child issue.
- `--remove-sub-issue <issue>` removes a child issue.

Manage issue dependencies with `gh issue create --blocked-by <issue> --blocking <issue>` or `gh issue edit`:

- `--add-blocked-by <issue>`
- `--remove-blocked-by <issue>`
- `--add-blocking <issue>`
- `--remove-blocking <issue>`

Read issue structure and dependency data:

```sh
gh issue view <issue-number-or-url> \
  --repo <owner>/<repo> \
  --json issueType,parent,subIssues,subIssuesSummary,blockedBy,blocking
```

Edit a field on an item, one value flag per call, matching the field type: `--text`, `--number`, `--date YYYY-MM-DD`, `--iteration-id`, `--single-select-option-id`, or `--clear`.

{{#if example}}
```sh
gh project item-edit \
  --id <item-id> \
  --project-id {{project.id}} \
  --field-id {{example.fieldId}} \
  --single-select-option-id {{example.optionId}}
```

{{/if}}
If the user names a value for a single-select field that isn't in the tables above, ask rather than picking the closest match.

Create a draft item:

```sh
gh project item-create {{project.number}} --owner team581 --title "<title>" --body "<body>"
```
